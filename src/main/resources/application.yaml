spring:
  application:
    name: MyFinances
  datasource:
    url: jdbc:mysql://localhost:3306/financas_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true